# LOD-3 — Foundation

One frame: the landing rendering after the Create React App to Vite migration, at 1440x900. The migration's job was to change the build and change nothing else, so a single frame proving the page still rendered was the proportionate evidence.

These frames are what shipped at that milestone. See `../README.md` for why
they are not re-shot to match the current site, and for the three defect
classes this method could not catch.
