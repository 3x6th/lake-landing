# LOD-4 — Experience

The cinematic hero across the viewport matrix, plus two frames of the engraving-to-ASCII passage at its two halves. Captured when the passage still lived inside the hero stage; it was later moved into its own full-bleed interlude, which the LOD-9 art-direction review records.

These frames are what shipped at that milestone. See `../README.md` for why
they are not re-shot to match the current site, and for the three defect
classes this method could not catch.
